#include "function_pointers.h"
#include <stdio.h>

/**
 * print_name.c - Function that prints a name
 * @name: pointer to a name
 * @f: pointer to function
 *
 * Return : name
 */

void print_name(char *name, void (*f)(char *))

	{
	if (name != null && f != null)
	return (name);
	}
